# CLAUDE.md

> 단일소스는 `AGENTS.md` (Codex 네이티브). Claude는 아래 import로 같은 불변식을 읽는다 — 크로스툴 동일 품질. (ccc-memory 단일소스 브리지.)
> 더해 **배포 always-on 가드**(쉬운말·참조와 부재·질문엔 답 등)도 import — 이 repo가 *자기 도구를 자신에게 직접 적용하도록*. `--plugin-dir` 없이 떠도 행동 가드가 살아있게(로드갭 봉합). 플러그인까지 로드되면 한 번 더 실리지만 무해.

@AGENTS.md
@plugins/agentoppa/always-on.md
